name: Windows

on:
  push:
    branches: [ main ]
    
  pull_request:
    branches: [ main ]
    
jobs:
  build:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v2
      
    - name: make setup
      run: mingw32-make setup
      shell: cmd
      
    - name: make bin/2DSpriteAnimation
      run: mingw32-make bin/2DSpriteAnimation
      shell: cmd
      
    - name: 'export binary'
      uses: actions/upload-artifact@v2
      with:
          name: 2DSpriteAnimation.exe
          path: bin/2DSpriteAnimation.exe
      
    - name: make clean
      run: mingw32-make clean
      shell: cmd

    - name: make bin/2DSpriteAnimation CXX=g++
      run: mingw32-make bin/2DSpriteAnimation CXX=g++
      shell: cmd
  
    - name: make clean
      run: mingw32-make clean
      shell: cmd
